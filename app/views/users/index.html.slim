h1 Profile Information

div 
| email: 
= @user.email 

div 
| username: 
= @user.username 

div 
| admin: 
= @user.admin 

div 
| country: 
= @user.country

div
| Avatar
-if current_user&.avatar.attached?
  = image_tag(current_user&.avatar, alt: "avatar user image", class: "item-center rounded-full h-32 w-32")
-else
  img.rounded-full.h-32.w-32[src="https://via.placeholder.com/124x124"]

div.m-2
  = link_to "Edit information", edit_user_registration_path, class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded"